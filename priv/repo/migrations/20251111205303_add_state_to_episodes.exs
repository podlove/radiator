defmodule Radiator.Repo.Migrations.AddStateToEpisodes do
  @moduledoc """
  Updates resources based on their most recent snapshots.

  This file was autogenerated with `mix ash_postgres.generate_migrations`
  """

  use Ecto.Migration

  def up do
    alter table(:episodes) do
      add :state, :text, null: false, default: "scheduling"
    end
  end

  def down do
    alter table(:episodes) do
      remove :state
    end
  end
end
