defmodule RadiatorWeb.Api.RaindropControllerTest do
  use RadiatorWeb.ConnCase, async: true

  describe "GET /raindrop/auth/redirect/:user_id" do


    # test "redirects to raindrop", %{conn: conn} do
    #   , user_id: user_id, code: code
    #   conn = get(conn, Routes.raindrop_auth_redirect_path(conn, :show, user_id: user_id, code: code))

    #   assert html_response(conn, 200) =~ "redirected"
    # end
  end
end
